class PlacementParams {
  PlacementParams(this.pieceId, this.row, this.col, this.rotationSteps, this.isFlipped);
  final String pieceId;
  final int row, col, rotationSteps;
  final bool isFlipped;
}

